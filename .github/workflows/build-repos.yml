name: Validate package repo

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"
  push:
    paths:
      - ".github/workflows/build-repos.yml"
      - "scripts/**"
      - "packages/**"
      - "metadata/**"
      - "README.md"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate shell syntax
        run: bash -n scripts/*.sh

      - name: Validate workflow YAML
        run: ruby -e 'require "yaml"; YAML.load_file(".github/workflows/build-repos.yml"); puts "ok"'

      - name: Validate package list format
        run: |
          set -euo pipefail
          normalized="$(
            sed -E 's/#.*$//' packages/extra.list \
              | awk '{$1=$1; if (NF) print $0}'
          )"
          duplicates="$(printf '%s\n' "${normalized}" | sort | uniq -d)"
          if [[ -n "${duplicates}" ]]; then
            echo "Duplicate packages found in packages/extra.list:"
            printf '%s\n' "${duplicates}" | sed 's/^/  - /'
            exit 1
          fi

      - name: Validate Arch repo scripts
        run: |
          set -euo pipefail
          docker run --rm \
            -e GITHUB_REPOSITORY_OWNER \
            -e GITHUB_REPOSITORY \
            -v "$GITHUB_WORKSPACE:/work" \
            -w /work \
            archlinux:latest \
            bash -lc '
              set -euo pipefail
              pacman-key --init
              pacman-key --populate archlinux
              if ! grep -q "^[[:space:]]*\\[multilib\\]" /etc/pacman.conf; then
                printf "\n[multilib]\nInclude = /etc/pacman.d/mirrorlist\n" >>/etc/pacman.conf
              fi
              pacman -Syu --noconfirm --needed archlinux-keyring base-devel git curl jq pacman-contrib
              useradd -m validator
              chown -R validator:validator /work
              su - validator -c "
                cd /work
                ./scripts/build.sh --dry-run
              "
            '

      - name: Validate publish script help
        run: bash scripts/publish.sh --help

# AUR packages for access-os-extra (one per line).
#
# Lines starting with # and blank lines are ignored.
neofetch
# mkinitcpio-firmware # TODO: requires AUR firmware deps (needs AUR dep resolver)
reiserfsprogs
